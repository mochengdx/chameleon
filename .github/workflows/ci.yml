name: CI

on:
  push:
    branches: [main, master, feat/adaptor_parse]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm (via official action)
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup Corepack (if needed)
        run: |
          npm install -g corepack

      - name: Enable and prepare pnpm
        run: |
          corepack enable
          corepack prepare pnpm@latest --activate

      - name: Verify pnpm is available
        run: pnpm --version

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Typecheck
        run: pnpm -w -s tsc --noEmit

      - name: Lint
        run: pnpm -w -s eslint . --ext .ts,.tsx,.js --max-warnings=0 || true

      - name: Run adapters tests
        run: pnpm --filter ./packages/adapters vitest run --reporter dot
