{
  "installCommand": "cd .. && corepack enable && corepack prepare pnpm@10.15.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && corepack enable && corepack prepare pnpm@10.15.0 --activate && pnpm --filter @chameleon/apps run build",
  "outputDirectory": "dist"
}
